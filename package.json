{
  "name": "party-local-host",
  "private": true,
  "version": "0.1.0",
  "description": "In-place Node host for the Party PHP app",
  "devDependencies": {
    "jest": "^30.0.5",
    "potrace": "^2.0.0",
    "puppeteer": "^24.7.2",
    "svgo": "^3.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "test:api": "jest --config jest.api.config.js --runInBand",
    "test:frontend": "jest --config jest.frontend.config.js --runInBand",
    "trace-icons": "node scripts/trace-icons.js"
  }
}
